name: best_one_golf
description: Golf score and settlement calculator
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.3.1
  go_router: ^17.2.0
  intl: ^0.20.2

  drift: ^2.20.3
  sqlite3_flutter_libs: ^0.6.0+eol
  path_provider: ^2.1.4
  path: ^1.9.0

  uuid: ^4.5.1

  rxdart: ^0.28.0

  google_fonts: ^8.0.2

  google_mobile_ads: ^7.0.0
  in_app_purchase: ^3.2.0
  package_info_plus: ^9.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

  build_runner: ^2.4.12
  drift_dev: ^2.20.3

  flutter_launcher_icons: ^0.14.1
  image: ^4.2.0

flutter:
  uses-material-design: true
  generate: true